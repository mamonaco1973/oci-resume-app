.terraform
*.tfstate
*.tfstate.backup
*.terraform.lock.hcl
*.terraform.*
*.pem
*.pub
*.zip

# Generated at deploy time from js/config.js.tmpl by apply.sh — never edit or
# commit the rendered file; the template is the source of truth.
04-webapp/js/config.js

# Redirected CLI output — never belongs in the repo
out.log

# draw.io autosave/backup scratch files (.$name.drawio.bkp, *.dtmp)
.$*
*.bkp
*.dtmp

# Local environment overrides (OCI_DOMAIN_NAME, OCI_SIGNUP_PROFILE_NAME, etc.)
env.sh


# Python bytecode
__pycache__/
*.pyc
